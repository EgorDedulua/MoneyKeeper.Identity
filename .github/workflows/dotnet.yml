name: .NET Test, Build & Push Docker Image

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

env:
  IMAGE_NAME: ${{ secrets.DOCKER_USERNAME }}/moneykeeper-identity  

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: dotnet restore src/MoneyKeeper.Identity/MoneyKeeper.Identity.csproj

      - name: Build
        run: dotnet build src/MoneyKeeper.Identity/MoneyKeeper.Identity.csproj --no-restore --configuration Release

      - name: Run tests
        run: dotnet test tests/MoneyKeeper.Identity.UnitTests/MoneyKeeper.Identity.UnitTests.csproj --no-build --configuration Release --verbosity normal

  build-and-push-docker-image:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha;mode=max
          